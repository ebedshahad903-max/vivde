# Preset data

`preset.json` is a placeholder manifest for the static Vivid Studio showcase. Each preset contains:

- a local fixed video path
- a card thumbnail
- four output thumbnail paths
- four caption tones
- fixed captions in nine languages

The browser reads this file directly. It does not contact a generation backend.
